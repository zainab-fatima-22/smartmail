# SmartMail — Day 1 Python dependencies (ML only)
# Backend (FastAPI, SQLAlchemy, etc.) will be added in requirements.txt
# updates on Day 2.

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
pytest>=7.4
