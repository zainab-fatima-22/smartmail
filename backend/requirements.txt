# SmartMail — Day 2 Backend dependencies
# Combine with the top-level requirements.txt (ML deps) when setting up
# your full environment: pip install -r requirements.txt -r backend/requirements.txt

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
sqlalchemy>=2.0
httpx>=0.27
pytest>=7.4
